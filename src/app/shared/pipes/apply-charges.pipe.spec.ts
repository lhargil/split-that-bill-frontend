import { ApplyChargesPipe } from './apply-charges.pipe';

describe('ApplyChargesPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplyChargesPipe();
    expect(pipe).toBeTruthy();
  });
});
